#pragma once

#define CLK_PREIOD_PS       (10000) // 100 MHz
#define CLK_HALF_PERIOD_PS  (CLK_PREIOD_PS / 2)

#define RAM_LOAD_OFFSET     (0x00000000)

// Note: memory mapping configuration is in memmap.h
